{% extends 'base_admin.html.twig' %}

{% block title %}Liste des Biens!{% endblock %}

{% block body %}

     <div class = "jumbotron">
          <h2> Users Interface Admin !</h2>
     </div>

 
     <div class = "container m-10">
     
          <div class = "row">
               {% for users in user %}


                    <div class = "col-md-6">
                         <article>

                              <h5>{{ user.username}}</h5>

                        
                         </article> 
                    </div> 
               {% endfor %}         
          </div>    


          
          <div class="pagination">
                         {{ knp_pagination_render(user) }}
          </div>
     </div>
{% endblock %}